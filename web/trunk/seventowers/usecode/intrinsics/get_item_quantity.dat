<?php
	text("Gets the quantity of the desired object.");
	make_header("Parameters", 0);
	add_keylist(
		array(
				inline_code("obj"), "The object whose quantity is desired.",
				inline_code("unk"), "An unknown parameter from the original games. Can be omitted if desired, as Exult does not use it.",
        	), 4
        );
	make_header("Return", 0);
	text("The quantity of the object if it has it, '1' otherwise.");
?>
