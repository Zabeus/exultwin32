<?php
	text("Inspects a NPC's temperature. The NPC's temperature changes over time depending on whether or not his FREEZE flag is set.");
	make_header("Parameters", 0);
	add_keylist(
		array(
				inline_code("npc"), "The NPC whose temperature is sought.",
        	), 4
        );
	make_header("Return", 0);
	text("A numeric value in the 0-63 range specifying how cold the NPC is; 0 being normal temperature and 63 being freezing cold.");
?>
