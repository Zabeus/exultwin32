<?php
	text("Removes a key from the keyring. Note that the keyring is a global object; multiple keyrings will share the same list of keys.");
	make_header("Parameters", 0);
	add_keylist(
		array(
				inline_code("qual"), "The quality of the key to be removed.",
        	), 4
        );
	make_header("Return", 0);
	text("If the key was on the keyring, returns $true; otherwise, returns $false.");
?>
