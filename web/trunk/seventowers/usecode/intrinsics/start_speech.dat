<?php
	text("Plays the specified speech track.");
	add_box(
    	array(
				"SI SPECIFIC",
				"In SI, speech also displays a full-screen face from 'FACES.VGA'."
			)
		);
	make_header("Parameters", 0);
	add_keylist(
		array(
				inline_code("track"), "Number indicating what speech track is to be played.",
        	), 4
        );
	make_header("Return", 0);
	text("Returns $false if failed to play the requested speech track (for example, if speech is disabled), $true otherwise.");
?>
