<?php
	text("Checks if a spellbook has one particular spell.");
	make_header("Parameters", 0);
	add_keylist(
		array(
				inline_code("obj"), "The spellbook to be inspected.",
				inline_code("index"), "The spell to be checked. This index starts at zero, and must be in the 0-71 range.",
        	), 4
        );
	make_header("Return", 0);
	text("$true if the supplied object is a spellbook and contains the spell; $false otherwise.");
?>
