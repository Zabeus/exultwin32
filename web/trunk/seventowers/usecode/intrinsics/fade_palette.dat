<?php
	text("Causes the screen to fade to/from black.");
	make_header("Parameters", 0);
	add_keylist(
		array(
				inline_code("length"), "The length, in ticks, of the fade; that is, how long it will take for the fade to happen.",
				inline_code("unk"), "Reserved; must be zero. This is an unknown parameter from the original games; it is unimplemented in Exult. It cannot be left out, however.",
				inline_code("type"), "The type of fade. If $true, fades from black; if $false, fades to black.",
        	), 4
        );
?>
