<?php
	text("Checks if a key is in the keyring. Note that the keyring is a global object; multiple keyrings will share the same list of keys.");
	make_header("Parameters", 0);
	add_keylist(
		array(
				inline_code("qual"), "The quality of the key to be checked.",
        	), 4
        );
	make_header("Return", 0);
	text("$true if the key is on the keyring, $false otherwise.");
?>
