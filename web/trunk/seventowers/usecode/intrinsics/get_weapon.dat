<?php
	text("Inspect what type of weapon an NPC is using.");
	make_header("Parameters", 0);
	add_keylist(
		array(
				inline_code("npc"), "The NPC whose weapon is to be inspected.",
        	), 4
        );
	make_header("Return", 0);
	text("Zero if no weapon, one- or two-handed, is wielded by the NPC. Otherwise, the shape in 'SHAPES.VGA' of the weapon being used.");
?>
