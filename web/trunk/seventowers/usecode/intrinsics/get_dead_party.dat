<?php
	text("Gets a list of all dead party members near a given object.");
	make_header("Parameters", 0);
	add_keylist(
		array(
				inline_code("obj"), "The object around which dead bodies will be searched.",
        	), 4
        );
	make_header("Return", 0);
	text("An array containing object references to the bodies of all dead party members within 50 tiles of the given object, or zero if no party members were found.");
?>
