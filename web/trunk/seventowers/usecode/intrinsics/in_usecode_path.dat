<?php
	text("Checks whether or not the actor is transversing a path due to " . inline_code("path_run_usecode") . " or " . inline_code("si_path_run_usecode") . " inteinsics.");
	make_header("Parameters", 0);
	add_keylist(
		array(
				inline_code("npc"), "The NPC to be checked.",
        	), 4
        );
	make_header("Return", 0);
	text("$true if the NPC is still walking a usecode path, $false otherwise.");
?>
