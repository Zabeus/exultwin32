<?php
	text("Marks the given light source as being added to or removed from its containing NPC.");
	make_header("Parameters", 0);
	add_keylist(
		array(
				inline_code("obj"), "The light source that is being added or removed.",
				inline_code("onoff"), "Flag specifying whether we want to add or remove the light source: $true to add it to its containing NPC, $false to remove it.",
        	), 4
        );
?>
