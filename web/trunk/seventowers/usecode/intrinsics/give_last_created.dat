<?php
	text("Gives the last item in the 'last created' stack to the specified container or NPC. This intrinsic ignores volume, and is quite likely to succeed; the given object is popped from the 'last created' stack only if the intrinsic succeeds.");
	make_header("Parameters", 0);
	add_keylist(
		array(
				inline_code("obj"), "Container or NPC that will gain the object.",
        	), 4
        );
	make_header("Return", 0);
	text("$true if successful, $false otherwise.");
?>
