<?php
	text("Checks if the NPC is dead or alive. This is equivalent to checking if the '" . inline_code("DEAD") . "' flag is set.");
	make_header("Parameters", 0);
	add_keylist(
		array(
				inline_code("npc"), "Who we want to check.",
        	), 4
        );
	make_header("Return", 0);
	text("Returns $true if the NPC is dead, $false otherwise.");
?>
