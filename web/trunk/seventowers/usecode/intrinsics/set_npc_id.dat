<?php
	text("Changes an NPC's identity. This identity has no real effect other than that given to it by usecode.");
	make_header("Parameters", 0);
	add_keylist(
		array(
				inline_code("npc"), "The NPC whose identity is to be changed.",
				inline_code("id"), "The new identity value. Must be an integer in the 0-31 range; values outside this range are accepted but incorrectly saved.",
        	), 4
        );
?>
