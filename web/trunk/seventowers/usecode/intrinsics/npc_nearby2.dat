<?php
	text("Finds out if the desired object is nearby. In this context, 'nearby' means within 40 tiles of the avatar. Note that, despite the name, this intrinsic is valid for all objects, not just NPCs.");
	make_header("Parameters", 0);
	add_keylist(
		array(
				inline_code("obj"), "The object to be checked.",
        	), 4
        );
	make_header("Return", 0);
	text("$true if the object is within 40 tiles of the avatar, $false otherwise.");
?>
