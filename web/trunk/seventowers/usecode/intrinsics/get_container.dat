<?php
	text("Gets the container of the supplied object.");
	make_header("Parameters", 0);
	add_keylist(
		array(
				inline_code("obj"), "The object whose container is desired.",
        	), 4
        );
	make_header("Return", 0);
	text("Return is zero if the object is directly in the game world, or an object reference to its immediate container otherwise.");
?>
