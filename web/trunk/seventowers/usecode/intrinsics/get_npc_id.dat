<?php
	text("Retrieves a NPC's identity. This identity has no real effect other than that given to it by usecode.");
	make_header("Parameters", 0);
	add_keylist(
		array(
				inline_code("npc"), "The NPC whose identity is sought.",
        	), 4
        );
	make_header("Return", 0);
	text("If given a valid NPC, this intrinsic returns that NPC's identity; otherwise it returns zero.");
?>
