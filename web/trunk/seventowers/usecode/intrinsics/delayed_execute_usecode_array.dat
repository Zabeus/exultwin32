<?php
	text("Deprecated; use $ucc script blocks instead. Animates an object according to the supplied script after a delay. Works exactly like " . inline_code("execute_usecode_array") . ", but starts only after the specified delay.");
	make_header("Parameters", 0);
	add_keylist(
		array(
				inline_code("obj"), "The object to be animated.",
				inline_code("script"), "The sequence of actions to be executed.",
				inline_code("delay"), "The number of ticks that must elapse before the animation starts.",
        	), 4
        );
	make_header("Return", 0);
	text("Always returns '1'.");
?>
