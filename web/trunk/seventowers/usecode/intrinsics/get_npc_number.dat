<?php
	make_header("Parameters", 0);
	add_keylist(
		array(
				inline_code("npc"), "The NPC whose number is desired. This is intended to receive an object reference, but accepts integers as normal.",
        	), 4
        );
	make_header("Return", 0);
	text("The NPC's number. This is a negative number for all 'true' NPCs, -356 for the avatar, 1 for monsters or zero for anything that is not a valid NPC.");
?>
