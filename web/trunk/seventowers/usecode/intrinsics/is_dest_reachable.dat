<?php
	text("Checks if a NPC can walk from its present location to a specified position.");
	make_header("Parameters", 0);
	add_keylist(
		array(
				inline_code("npc"), "The NPC we wish to check.",
				inline_code("pos"), "Where we want check if the NPC can go.",
        	), 4
        );
	make_header("Return", 0);
	text("$true if the NPC can walk to the supplied destination, $false otherwise.");
?>
