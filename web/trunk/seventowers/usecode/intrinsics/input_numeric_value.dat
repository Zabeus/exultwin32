<?php
	text("Prompts the player for numerical input with the bar slider.");
	make_header("Parameters", 0);
	add_keylist(
		array(
				inline_code("min"), "The lowest number allowed.",
				inline_code("max"), "The highest number allowed.",
				inline_code("step"), "The distance between any two consecutive numbers that can be chosen.",
				inline_code("default"), "The initial value selected; this value is forced to conform to the boundaries specified above.",
        	), 4
        );
	make_header("Return", 0);
	text("The number chosen by the player.");
?>
