<?php
	text("Adds a key to the keyring. Note that the keyring is a global object; multiple keyrings will share the same list of keys.");
	make_header("Parameters", 0);
	add_keylist(
		array(
				inline_code("qual"), "The quality of the key to be added.",
        	), 4
        );
?>
