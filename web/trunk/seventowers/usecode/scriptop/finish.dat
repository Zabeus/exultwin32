<?php
	code_block("finish");
	text("Immediatelly executes a second command. Additionally, if an actor's scripts are deleted (for example, because the actor has been cached out), this command forces the quick execution of all remaining script commands.");
?>
