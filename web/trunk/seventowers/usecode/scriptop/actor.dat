<?php
	code_block("actor frame framenum");
	text("Sets the actor's frame to 'framenum' while keeping the actor's facing. Only the first 4 bits of 'framenum' are used when compiling this opcode, so you may as well use a number in the 0-15 range. Compare with '" . inline_code("frame") . "' command.");
?>
